<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wagh.laundry">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".placeorder"></activity>
        <activity android:name=".placeorderfinal"/>
        <activity android:name=".history"/>
        <activity android:name=".checkstatus"/>
        <activity android:name=".offers"/>
        <activity android:name=".delete"/>
        <activity android:name=".pricing"/>
    </application>

</manifest>
